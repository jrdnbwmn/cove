# Models

Jumpstart's core models live in `app/models`. These are the User and Account models which provide the base functionality for your application.

Features are organized in concerns to keep things organized. Concerns and additional models can be found in `lib/jumpstart/app/models`
