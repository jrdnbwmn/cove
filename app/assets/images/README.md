# Images

Jumpstart uses two images for your logo:

- logo.svg
- mark.svg (square icon)

To override the Jumpstart icons, simply add these files to `app/assets/images`

## Colors

We recommend using `fill="currentColor"` in your SVG so that CSS colors can be applied.

