<% unless user_signed_in? %>
  <div class="flex flex-col gap-2 lg:flex-row">
    <%= render ButtonComponent.new(text: t(".log_in"), variant: :secondary, href: new_user_session_path) %>
    <%= render ButtonComponent.new(text: t(".sign_up"), variant: :primary, href: new_user_registration_path) %>
  </div>
<% end %>
