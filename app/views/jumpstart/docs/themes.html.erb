<% content_for :title, "Themes" %>

<h2>Light-only interface</h2>

<p>Cove uses a single, light-only theme.</p>

<p>
  Components use semantic color tokens so the interface remains visually consistent without a user-selectable theme.
</p>
