<div class="container mx-auto p-4">
  <div class="my-8">
    <h1>Subjects</h1>

    <%= render EmptyStateComponent.new(title: "Coming soon", description: "Subject management will be available here.") do |c| %>
      <% c.with_icon do %>
        <%= icon "book-open", class: "w-full h-full" %>
      <% end %>
    <% end %>
  </div>
</div>
