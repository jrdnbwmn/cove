<div class="container mx-auto p-4">
  <div class="my-8">
    <h1>Schedules</h1>

    <%= render EmptyStateComponent.new(title: "Coming soon", description: "Schedule planning will be available here.") do |c| %>
      <% c.with_icon do %>
        <%= icon "calendar", class: "w-full h-full" %>
      <% end %>
    <% end %>
  </div>
</div>
